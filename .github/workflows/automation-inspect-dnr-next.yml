name: automation inspect next lossless DNR parity case
on:
  push:
    branches: [dev]
    paths: ['.github/workflows/automation-inspect-dnr-next.yml']
permissions:
  contents: read
jobs:
  inspect:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Inspect compiler and parity fixtures
        shell: bash
        run: |
          echo '=== compiler ==='
          sed -n '1,380p' src/main/backends/dnr/compiler.js
          for f in test/dnr-firefox-filter-parity.test.js test/dnr-tld-parity.test.js test/dnr-method-parity.test.js test/dnr-action-parity.test.js test/dnr-url-boundaries.test.js test/dnr-compiler.test.js docs/mv3-supported-subset.md; do
            echo "=== $f ==="; sed -n '1,380p' "$f" || true
          done
